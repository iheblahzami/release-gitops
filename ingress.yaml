apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: releaseapp-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: 
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: releaseapp-service
            port:
              number: 80
